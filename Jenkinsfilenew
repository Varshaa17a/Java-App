pipeline {
    agent any   

    stages {
        stage("Code Checkout") {
            steps {
                checkout scm
            }
        }

        stage("Compile Code") {
            steps {
                sh "mvn clean package ${MAVEN_OPTS}"
            }
        }
}
}
